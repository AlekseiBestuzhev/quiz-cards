import { Button } from '@/components/ui/button'

export const App = () => {
  return (
    <div>
      <p style={{ color: 'var(--color-info-500)' }}>Hello</p>
      <p style={{ color: 'var(--color-danger-500)' }}>Hello</p>
      <Button>Text</Button>
    </div>
  )
}
